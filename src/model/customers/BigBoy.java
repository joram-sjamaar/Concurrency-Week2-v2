package model.customers;

import model.PrinterManager;

public class BigBoy extends Customer{

    public BigBoy(String name, PrinterManager printerManager) {
        super(name, printerManager);
    }
}
